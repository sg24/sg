{
  "name": "index",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "uuid": "^3.4.0"
  },
  "scripts": {
    "build": "react-scripts build && react-snapshot"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
  },
  "reactSnap": {
    "inlineCss": true,
    "puppeteerArgs": ["--no-sandbox", "--disable-setuid-sandbox"]
  }
}
